{
  "private": true,
  "name": "betahaus-sofia-office-manager",
  "version": "1.2.0",
  "license": "MIT",
  "scripts": {
    "deploy": "npm run build && firebase deploy",
    "start": "serve ./src/index.html",
    "lint": "tslint \"./src/**/*.ts\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "compote": "0.6.1",
    "moment": "2.17.1"
  },
  "devDependencies": {
    "@types/moment": "2.13.0",
    "firebase-tools": "3.3.0",
    "serve": "3.3.1",
    "tslint": "4.4.2",
    "typescript": "2.1.6"
  }
}
