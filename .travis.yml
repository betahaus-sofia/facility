language: node_js

node_js: 6

branches:
  only:
    - master

cache:
  directories:
    - node_modules

script:
  - npm test
  - npm run build
  - npm run deploy -- --token $FIREBASE_TOKEN

after_success:
  - bash <(curl -s https://codecov.io/bash)
