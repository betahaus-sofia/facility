{
  "database": {
    "rules": {
      ".read": true,
      ".write": false,

      // Requests
      "requests": {
        ".write": true,

        "$request": {
          "date": { ".validate": "newData.val() === now" },
          "$other": { ".validate": false }
        }
      },

      // Supplies
      "supplies": {
        "$supply": {
          "name": { ".validate": "newData.isString()" },
          "imageUrl": { ".validate": "newData.isString()" },
          "requests": {
            ".write": true,
            "$request": { ".validate": "newData.isBoolean()" }
          },

          "$other": { ".validate": false }
        }
      },

      // Rooms
      "rooms": {
        "$room": {
          "name": { ".validate": "newData.isString()" },
          "supplies": {
            ".write": true,
            "$supply": { ".validate": "newData.isBoolean()" }
          },

          "$other": { ".validate": false }
        }
      },

      "$other": { ".validate": false }
    }
  },
  "hosting": {
    "public": "dist",
    "rewrites": [
      {
        "source": "**",
        "destination": "/index.html"
      }
    ]
  }
}
